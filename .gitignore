# Neovim generated files (lazy.nvim lock, plugin cache, state, etc.)
nvim/lazy-lock.json
nvim/plugin/

# Ghostty live config (machine-specific, modified by switch scripts)
ghostty/config
ghostty/*.bak

# OS files
.DS_Store
